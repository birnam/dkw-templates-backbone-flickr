define([

        'jquery',
        'underscore',
        'backbone'

], function(

        $,
        _,
        Backbone

) {

    var AbstractCollection = Backbone.Collection.extend({

    });

    return AbstractCollection;

});