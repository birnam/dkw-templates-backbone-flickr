define([

        'jquery',
        'underscore',
        'backbone'

], function(

        $,
        _,
        Backbone

) {

    var AbstractModel = Backbone.Model.extend({

    });

    return AbstractModel;

});